# 密码保护页面使用说明

## 快速开始

1. **复制模板文件**：将 `protected_page_template.html` 复制并重命名为您想要的页面名称，例如：
   - `private_photos.html`
   - `internal_docs.html`
   - `secret_project.html`

2. **设置密码**：打开文件，找到这一行（大约第115行）：
   ```javascript
   const CORRECT_PASSWORD = "your_password_here";
   ```
   将 `"your_password_here"` 替换为您想要的密码，例如：
   ```javascript
   const CORRECT_PASSWORD = "MySecret123";
   ```

3. **添加您的内容**：在 `<!-- Protected Content -->` 部分添加您想保护的内容。

## 功能特点

✅ **密码保护**：只有输入正确密码才能查看内容  
✅ **记住我功能**：可以选择7天内自动记住密码  
✅ **美观界面**：现代化的登录界面设计  
✅ **错误提示**：密码错误时会有友好提示  
✅ **回车提交**：支持按回车键提交密码  

## 安全说明

⚠️ **重要提示**：这是前端密码保护，密码存储在 JavaScript 代码中。这种方式：
- ✅ 适合防止普通访问者随意浏览
- ✅ 适合个人网站的简单保护
- ❌ **不适合**需要高安全性的内容（机密文档、商业机密等）
- ❌ 技术用户可以通过查看网页源代码找到密码

如果需要更高级的安全保护，建议使用：
- 后端服务器验证
- 使用 GitHub Pages 的私有仓库
- 使用专业的身份验证服务

## 自定义选项

### 修改记住密码的时长
找到这一行：
```javascript
const REMEMBER_DURATION = 7 * 24 * 60 * 60 * 1000; // 7天
```
可以修改为其他时长，例如：
- 1天：`1 * 24 * 60 * 60 * 1000`
- 30天：`30 * 24 * 60 * 60 * 1000`

### 添加退出登录功能
如果您想在页面中添加"退出"按钮，可以在受保护内容中添加：
```html
<button onclick="logout()">退出登录</button>
```

## 示例

查看 `protected_page_template.html` 获取完整示例。

## 问题反馈

如有问题或需要更多功能，请随时询问！
